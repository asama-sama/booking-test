import {Booking} from '../Booking';

// Takes a CSV format and transforms to a bookings object
const parseBookingsFromCSV = (bookingsCSV: string) : Booking[] => {
    return []
}

export {parseBookingsFromCSV};